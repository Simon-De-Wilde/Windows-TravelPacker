<Page x:ConnectionId='1' x:Class="TravelPacker.View.Travels.TravelsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:TravelPacker.View.Travels"
      xmlns:devicefam="using:TravelPacker.Util"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:model="using:TravelPacker.Model"
      xmlns:muxc="using:Windows.UI.Xaml.Controls"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      RequestedTheme="Light">



    <ScrollViewer>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:ConnectionId='2' x:Name="desktopView">
                    <VisualState.StateTriggers>
                        <devicefam:DeviceFamilyStateTrigger TargetDeviceFamily="Desktop" />
                        <AdaptiveTrigger MinWindowWidth="670" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="txt_header.HorizontalAlignment"
                                Value="Left" />
                        <Setter Target="btn_addTravel.HorizontalAlignment"
                                Value="Left" />
                        <Setter Target="TravelsGV.HorizontalAlignment"
                                Value="Left" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='3' x:Name="tabletView">
                    <VisualState.StateTriggers>
                        <devicefam:DeviceFamilyStateTrigger TargetDeviceFamily="Desktop" />
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="txt_header.HorizontalAlignment"
                                Value="Center" />
                        <Setter Target="btn_addTravel.HorizontalAlignment"
                                Value="Center" />
                        <Setter Target="TravelsGV.HorizontalAlignment"
                                Value="Center" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='4' x:Name="mobileView">
                    <VisualState.StateTriggers>
                        <devicefam:DeviceFamilyStateTrigger TargetDeviceFamily="Mobile" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="txt_header.HorizontalAlignment"
                                Value="Center" />
                        <Setter Target="btn_addTravel.HorizontalAlignment"
                                Value="Center" />
                        <Setter Target="TravelsGV.HorizontalAlignment"
                                Value="Center" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <StackPanel Orientation="Vertical">
            <TextBlock x:ConnectionId='5' x:Name="txt_header"
                       Text="Your Travels"
                       Style="{ThemeResource HeaderTextBlockStyle}"
                       HorizontalAlignment="Left" />
            <Button x:ConnectionId='6' x:Name="btn_addTravel"
                    Margin="0,10,0,0"
                                          
                    HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <SymbolIcon Symbol="Add"
                                Margin="5"
                                VerticalAlignment="Center" />
                    <TextBlock Text="Add new Travel"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <GridView x:ConnectionId='7' x:Name="TravelsGV"
                                                              
                      IsItemClickEnabled="True"
                      Margin="0,20,0,0"
                      HorizontalContentAlignment="Left"
                      HorizontalAlignment="Left"
                      IsRightTapEnabled="True"
                                               >
                <GridView.ItemTemplate>
                    <DataTemplate                          >
                        <StackPanel x:ConnectionId='8' x:Name="travelCard"
                                                                       
                                    IsRightTapEnabled="True"
                                    Background="White"
                                    Orientation="Horizontal"
                                    Width="450"
                                    MinHeight="160"
                                    BorderBrush="{ThemeResource ButtonBorderThemeBrush}"
                                    BorderThickness="3"
                                    CornerRadius="2"
                                    Margin="10">
                            <Image x:ConnectionId='9'                           
                                   Width="200"
                                   Margin="10" />
                            <StackPanel Orientation="Vertical"
                                        Padding="0,10,50,0"
                                        Width="220">
                                <TextBlock x:ConnectionId='10'                     
                                           TextWrapping="Wrap"
                                           Style="{ThemeResource TitleTextBlockStyle}" />
                                <TextBlock x:ConnectionId='11'                         
                                           TextWrapping="Wrap"
                                           Style="{ThemeResource SubtitleTextBlockStyle}" />
                                <muxc:ProgressBar x:ConnectionId='12'                           />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

        </StackPanel>
    </ScrollViewer>
</Page>

